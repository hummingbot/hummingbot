# list the Ethereum networks available to gateway
networks:
  # the name of the network
  mainnet:
    # the unique chain id of the network
    chainID: 1
    # the node url of the network
    nodeURL: http://localhost:8501/
  kovan:
    chainID: 42
    nodeURL: https://kovan.infura.io/v3/
    # an optional API key for the node, like Infura
    nodeApiKey: xyz123zir012312340123asdfne
