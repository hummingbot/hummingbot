import Channels from '../channels';
import { CHANNEL } from '../types';
declare function adjustChannel(color: string | Channels, channel: CHANNEL, amount: number): string;
export default adjustChannel;
