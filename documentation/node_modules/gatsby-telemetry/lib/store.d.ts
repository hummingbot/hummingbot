export declare class Store {
    bufferFilePath: string;
    baseDir: string;
    eventsJsonFileName: string;
    constructor(baseDir: string);
    appendToBuffer(event: unknown): void;
    flushFile(filePath: string, flushOperation: Function): Promise<boolean>;
    startFlushEvents(flushOperation: Function): Promise<boolean>;
}
