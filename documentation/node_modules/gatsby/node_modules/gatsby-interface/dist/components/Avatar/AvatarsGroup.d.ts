import { AvatarSize } from "./types";
import { AvatarProps } from "./Avatar";
export declare type AvatarDescriptor = Pick<AvatarProps, "src" | "label" | "fallback">;
export declare type AvatarsGroupProps = {
    avatars: AvatarDescriptor[];
    size?: AvatarSize;
    borderColor?: string;
    omittedAvatarsCount?: number;
    omittedAvatarsLabel?: string;
    className?: string;
    style?: React.CSSProperties;
};
export default function AvatarsGroup({ avatars, size, borderColor, omittedAvatarsCount, omittedAvatarsLabel, className, style, }: AvatarsGroupProps): JSX.Element;
