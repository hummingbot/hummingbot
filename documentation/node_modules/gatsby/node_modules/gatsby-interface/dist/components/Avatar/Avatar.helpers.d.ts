declare type FitTextOptions = {
    maxWidth: number;
    minFontSizeInRem: number;
    maxFontSizeInRem: number;
};
export declare function fitText<T extends HTMLElement>({ maxWidth: maxTextWidth, minFontSizeInRem, maxFontSizeInRem, }: FitTextOptions): (element: T | null) => void;
export {};
