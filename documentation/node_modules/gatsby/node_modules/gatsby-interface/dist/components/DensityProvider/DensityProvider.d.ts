import React from "react";
export declare type Density = `DENSE` | `DEFAULT` | `LOOSE`;
export declare type DensityContextValue = Density;
export declare type DensityProviderProps = {
    density?: Density;
    children?: React.ReactNode;
};
export declare function DensityProvider({ density, children, }: DensityProviderProps): JSX.Element;
export declare function useDensity(): Density;
