export declare type ConcealedValueProps = {
    value: string;
    ariaLabel: string;
    delay?: number;
    concealed?: boolean;
};
export declare function ConcealedValue({ value, ariaLabel, concealed, delay, }: ConcealedValueProps): JSX.Element;
