import React from "react";
import { ToastTone } from "./types";
export interface ToastOptions {
    tone?: ToastTone;
    timeout: number;
}
export interface ToastContextDefinition {
    showToast: (message: React.ReactNode, toastArg?: ToastOptions) => void;
}
export declare const ToastContext: React.Context<ToastContextDefinition>;
export declare function useToastContext(): ToastContextDefinition;
