import { BaseTextProps } from "../BaseText";
import { TextTone, TextVariant, TextSize } from "./types";
export declare type TextProps = BaseTextProps & {
    tone?: TextTone;
    variant?: TextVariant;
    size?: TextSize;
};
declare function Text({ tone, variant, size, ...rest }: TextProps): JSX.Element;
export default Text;
