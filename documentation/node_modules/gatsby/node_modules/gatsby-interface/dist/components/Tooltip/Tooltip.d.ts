import React from "react";
import { TooltipPosition } from "./types";
export declare type TooltipProps = Omit<JSX.IntrinsicElements["div"], "ref" | "label" | "children"> & {
    label: React.ReactNode;
    children: React.ReactNode;
    position?: TooltipPosition;
};
export default function Tooltip({ children, id, label, ...rest }: TooltipProps): JSX.Element;
