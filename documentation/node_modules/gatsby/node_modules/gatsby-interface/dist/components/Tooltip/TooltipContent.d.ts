import React from "react";
import { TooltipParams } from "@reach/tooltip";
import { TooltipPosition } from "./types";
import { TooltipPointerProps } from "./TooltipPointer";
declare type TooltipContentProps = Omit<JSX.IntrinsicElements["div"], "label" | "children"> & {
    label: React.ReactNode;
    tooltipParams: TooltipParams;
    position?: TooltipPosition;
} & Pick<TooltipPointerProps, "style">;
export default function TooltipContent({ position, label, tooltipParams, "aria-label": ariaLabel, style, ...rest }: TooltipContentProps): JSX.Element;
export {};
