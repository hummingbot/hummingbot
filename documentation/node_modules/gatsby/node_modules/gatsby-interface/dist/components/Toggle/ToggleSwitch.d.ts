import React from "react";
import { AtomTone } from "../../theme/types";
export declare type ToggleSwitchProps = Omit<JSX.IntrinsicElements["input"], "id" | "value"> & {
    id: string;
    value: string;
    valueOn: string;
    valueOff: string;
    labelOn: React.ReactNode;
    labelOff: React.ReactNode;
    tone?: AtomTone;
    className?: string;
    style?: React.CSSProperties;
};
export default function ToggleSwitch({ id, valueOn, valueOff, labelOn, labelOff, value, tone, className, style, ...rest }: ToggleSwitchProps): JSX.Element;
