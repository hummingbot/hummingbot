function warn(message) {
  var level = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "warning";

  if (process.env.NODE_ENV === "development") {
    if (level === "error") {
      console.error(message);
    } else {
      console.warn(message);
    }
  }
}

export { warn };
//# sourceMappingURL=warn.esm.js.map
