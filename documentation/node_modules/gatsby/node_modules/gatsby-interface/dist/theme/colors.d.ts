import { Colors as DesignColors } from "gatsby-design-tokens";
export declare type Colors = DesignColors & {
    primaryBackground: string;
    secondaryBackground: string;
    standardLine: string;
};
declare const colors: Colors;
export default colors;
