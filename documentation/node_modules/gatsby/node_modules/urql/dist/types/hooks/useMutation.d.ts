import { DocumentNode } from 'graphql';
import { OperationResult, OperationContext, CombinedError, Operation } from '@urql/core';
export interface UseMutationState<T> {
    fetching: boolean;
    stale: boolean;
    data?: T;
    error?: CombinedError;
    extensions?: Record<string, any>;
    operation?: Operation;
}
export declare type UseMutationResponse<T, V> = [
    UseMutationState<T>,
    (variables?: V, context?: Partial<OperationContext>) => Promise<OperationResult<T>>
];
export declare function useMutation<T = any, V = object>(query: DocumentNode | string): UseMutationResponse<T, V>;
