import { ExecutionResult } from '@graphql-tools/utils';
import { ExecutionParams, Endpoint } from './types';
export declare const getBatchingExecutor: (A1: Record<string, any>, A2: Endpoint<any, any, any>, A3: ({ document, context, variables, info }: ExecutionParams) => ExecutionResult | Promise<ExecutionResult>) => (executionParams: ExecutionParams) => Promise<any>;
