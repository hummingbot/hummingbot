import { GraphQLSchema, SelectionSetNode, TypeInfo, GraphQLOutputType } from 'graphql';
import { Transform, Request } from '@graphql-tools/utils';
export default class VisitSelectionSets implements Transform {
    private readonly schema;
    private readonly initialType;
    private readonly visitor;
    constructor(schema: GraphQLSchema, initialType: GraphQLOutputType, visitor: (node: SelectionSetNode, typeInfo: TypeInfo) => SelectionSetNode);
    transformRequest(originalRequest: Request): Request;
}
