import React from "react";
import { AbstractTooltip } from "../AbstractTooltip";
import { MenuConfigProvider } from "../MenuConfig";
import { ShadedColor } from "../colors";
interface Props extends Pick<React.ComponentProps<typeof AbstractTooltip>, "children" | "content" | "maxWidth" | "placement" | "trigger" | "fallbackPlacements" | "popperOptions">, Omit<React.ComponentProps<typeof MenuConfigProvider>, "children"> {
    className?: string;
    /**
     * Close menu automatically when a `MenuItem` is clicked
     *
     * @default true
     */
    closeOnMenuItemClick?: boolean;
    /**
     * Color to use for the selected state of a menu item. Hover state will be
     * programatically calculated depending on the theme.
     */
    color?: ShadedColor;
    style?: React.CSSProperties;
}
/**
 * Menu element
 */
export declare const Menu: React.FC<Props>;
export {};
