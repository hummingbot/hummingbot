import { Instance } from "tippy.js";
export declare const mouseRest: {
    name: string;
    defaultValue: boolean;
    fn(instance: Instance): {
        onCreate(): void;
        onDestroy(): void;
    };
};
